@function can-fixture/StoreType Store
@parent can-fixture.types
@group can-fixture/StoreType.prototype prototype

@signature `Store`

The following documents the methods on a store object returned by [can-fixture.store].
